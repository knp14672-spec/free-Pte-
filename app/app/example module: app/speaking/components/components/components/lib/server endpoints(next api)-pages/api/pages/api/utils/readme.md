# PTE Portal (Starter)

## Setup (local)
1. copy `.env.example` → `.env.local` and fill keys.
2. npm install
3. npm run dev
4. API endpoints: /api/transcribe and /api/score are server placeholders.

## Deploy
- Push to GitHub and connect repository to Vercel.
- Add environment variables in Vercel dashboard.
